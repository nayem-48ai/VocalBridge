// path: settings.gradle
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // এটি কনফ্লিক্ট ফিক্স করবে
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "VocalBridge"
include ':app'
